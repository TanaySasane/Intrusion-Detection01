version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - SECRET_KEY=change-this-in-production
      - FLASK_ENV=production
    volumes:
      - ./database:/app/database
    restart: unless-stopped
